# Reproducibility

Publishable benchmark runs record repository commit SHA, case ID, model/configuration, exact prompts, source fingerprints, raw outputs, scorer output, elapsed time, tool calls, and token usage when available. Gold expectations remain hidden. Fresh contexts are used for matched comparisons. Failed and ambiguous runs remain in the study record.
